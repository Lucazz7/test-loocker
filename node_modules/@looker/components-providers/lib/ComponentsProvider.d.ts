import { CoreColors } from '@looker/design-tokens';
import { FC } from 'react';
import { ThemeProviderProps } from './ThemeProvider';
export interface ComponentsProviderProps extends ThemeProviderProps {
    globalStyle?: boolean;
    ie11Support?: boolean;
    coreColors?: Partial<CoreColors>;
}
export declare const ComponentsProvider: FC<ComponentsProviderProps>;
