import { ExtensionHostApi, ExtensionHostConfiguration } from './types';
/**
 * Connect extension to Looker host. React extensions using the extension-sdk-react
 * package and ExtensionProvider do not need to call this as the ExtensionProvider
 * sets up the connection.
 * @param configuration
 */
export declare const connectExtensionHost: (configuration?: ExtensionHostConfiguration) => Promise<ExtensionHostApi>;
