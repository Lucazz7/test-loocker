import React from 'react';
import { BorderProps, SpaceProps, CompatibleHTMLProps } from '@looker/design-tokens';
import { ValidationType } from '../../ValidationMessage';
import { TimeFormats } from '../utils';
declare type intervals = 5 | 10 | 15 | 30;
export interface InputTimeSelectProps extends SpaceProps, BorderProps, Omit<CompatibleHTMLProps<HTMLDivElement>, 'value' | 'onChange'> {
    format?: TimeFormats;
    interval?: intervals;
    defaultValue?: string;
    value?: string;
    onChange?: (val?: string) => void;
    validationType?: ValidationType;
    onValidationFail?: (value: string) => void;
}
export declare const InputTimeSelect: import("styled-components").StyledComponent<React.ForwardRefExoticComponent<InputTimeSelectProps & React.RefAttributes<HTMLDivElement>>, any, {}, never>;
export {};
