"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.moveFocus = void 0;

var getTabStops = ref => Array.from(ref.querySelectorAll('a,button,[tabindex="0"]'));

var moveFocus = (direction, initial, containerRef) => {
  if (!containerRef || !containerRef.current) return;
  var tabStops = getTabStops(containerRef.current);

  if (document.activeElement && tabStops.includes(document.activeElement)) {
    var next = tabStops.findIndex(f => f === document.activeElement) + direction;
    if (next === tabStops.length) return;
    if (!tabStops[next]) return;
    tabStops[next].focus();
  } else {
    tabStops.slice(initial)[0].focus();
  }

  return false;
};

exports.moveFocus = moveFocus;
//# sourceMappingURL=moveFocus.js.map