"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.useElementVisibility = void 0;

var _react = require("react");

var useElementVisibility = ref => {
  var [isVisible, setIsVisible] = (0, _react.useState)(true);
  var observer = typeof IntersectionObserver === 'undefined' ? null : new IntersectionObserver((_ref) => {
    var [entry] = _ref;
    setIsVisible(entry.intersectionRatio > 0);
  }, {
    threshold: [0, 1]
  });
  (0, _react.useEffect)(() => {
    var refCurrent = ref.current;

    if (refCurrent) {
      observer && observer.observe(refCurrent);
    }

    return () => {
      if (refCurrent) {
        observer && observer.unobserve(refCurrent);
      }
    };
  }, [observer, ref]);
  return isVisible;
};

exports.useElementVisibility = useElementVisibility;
//# sourceMappingURL=MenuGroup.hooks.js.map