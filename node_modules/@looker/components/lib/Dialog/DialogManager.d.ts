import { FC, ReactNode } from 'react';
import { DialogProps } from './Dialog';
declare type DialogRenderProp = (dialogProps: {
    onClick: () => void;
    className?: string;
    role?: string;
    'aria-expanded'?: boolean;
}) => ReactNode;
export interface DialogManagerProps extends DialogProps {
    children?: DialogRenderProp | ReactNode;
    content: ReactNode;
    canClose?: () => boolean;
}
export declare const DialogManager: FC<DialogManagerProps>;
export {};
