import React, { ReactNode } from 'react';
import { SpacingSizes } from '@looker/design-tokens';
import { IconNames, IconSize } from '../Icon';
import { SimpleLayoutProps } from '../Layout/utils/simple';
export declare type IndicatorIcons = {
    close: IconNames;
    open: IconNames;
};
export interface AccordionIndicatorProps {
    indicatorPosition?: 'left' | 'right';
    indicatorSize?: IconSize;
    indicatorGap?: SpacingSizes;
    indicatorIcons?: IndicatorIcons;
}
export interface AccordionControlProps {
    defaultOpen?: boolean;
    isOpen?: boolean;
    toggleOpen?: (isOpen: boolean) => void;
    onClose?: () => void;
    onOpen?: () => void;
}
export declare const AccordionIndicatorPropKeys: string[];
export declare const AccordionControlPropKeys: string[];
export interface AccordionProps extends AccordionControlProps, AccordionIndicatorProps, SimpleLayoutProps {
    children: ReactNode;
    className?: string;
}
export declare const Accordion: import("styled-components").StyledComponent<React.FC<AccordionProps>, any, {}, never>;
