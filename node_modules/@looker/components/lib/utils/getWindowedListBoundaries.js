"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.getWindowedListBoundaries = getWindowedListBoundaries;

function getWindowedListBoundaries(_ref) {
  var {
    buffer = 5,
    containerHeight,
    containerScrollPosition,
    enabled = true,
    itemHeight,
    length
  } = _ref;
  if (!enabled) return {
    end: length - 1,
    start: 0
  };
  if (containerScrollPosition === undefined || containerHeight === undefined) return {
      end: 0,
      start: 0
    };
  var top = Math.floor(containerScrollPosition / itemHeight);
  var bottom = Math.ceil((containerHeight + containerScrollPosition) / itemHeight);
  return {
    end: bottom + buffer > length - 1 ? length - 1 : bottom + buffer,
    start: top - buffer < 0 ? 0 : top - buffer
  };
}
//# sourceMappingURL=getWindowedListBoundaries.js.map