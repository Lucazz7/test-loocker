"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.tintOrShadeUiColor = void 0;

var _polished = require("polished");

var _scaleMixAmount = require("./scaleMixAmount");

var tintOrShadeUiColor = (mixAmount, color) => {
  var colorLuminance = (0, _polished.getLuminance)(color);
  var mixAdjustment = colorLuminance > 0.5 ? mixAmount : (0, _scaleMixAmount.scaleMixAmount)(mixAmount, 1.5);

  if (colorLuminance > 0.5) {
    return (0, _polished.shade)(mixAdjustment / 100, color);
  } else {
    return (0, _polished.tint)(mixAdjustment / 100, color);
  }
};

exports.tintOrShadeUiColor = tintOrShadeUiColor;
//# sourceMappingURL=tintOrShadeUiColor.js.map