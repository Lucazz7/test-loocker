import { StatefulColors, SpecifiableColors } from '../../system/color';
export declare const generateInteractive: (color: string) => string;
export declare const generatePressed: (color: string) => string;
export declare const generateStatefulColors: (colors: SpecifiableColors) => StatefulColors;
